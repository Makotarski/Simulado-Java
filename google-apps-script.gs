function doPost(e){const lock=LockService.getScriptLock();lock.waitLock(10000);try{const ss=SpreadsheetApp.getActiveSpreadsheet();const sheet=ss.getSheetByName('Resultados')||ss.insertSheet('Resultados');if(sheet.getLastRow()===0){sheet.appendRow(['Data e hora','Nome','Regra do nome','Nível 1','Nível 2','Nível 3','Acertos nas questões','Pontuação total','Duração (s)','Respostas completas']);sheet.getRange(1,1,1,10).setFontWeight('bold').setBackground('#10233f').setFontColor('#ffffff');sheet.setFrozenRows(1)}const d=JSON.parse(e.postData.contents);sheet.appendRow([new Date(),d.nome,d.regraNome,d.nivel1,d.nivel2,d.nivel3,d.acertosQuestoes,d.pontuacaoTotal,d.duracaoSegundos,JSON.stringify(d.respostas)]);sheet.autoResizeColumns(1,9);return ContentService.createTextOutput(JSON.stringify({ok:true})).setMimeType(ContentService.MimeType.JSON)}finally{lock.releaseLock()}}
function doGet(){return ContentService.createTextOutput('Coletor do Simulado TOFAS ativo.')}
